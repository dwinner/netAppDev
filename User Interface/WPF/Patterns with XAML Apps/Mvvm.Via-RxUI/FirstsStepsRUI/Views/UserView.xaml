<UserControl x:Class="FirstsStepsRUI.Views.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <TextBlock Text="UserName" />
            <TextBox x:Name="UserName" />
            <TextBlock Text="Group" />
            <ComboBox x:Name="Group">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource UserGroupConverter}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <TextBlock Text="Write something here:" />
            <TextBox x:Name="Write" />
            <Button x:Name="Submit" Content="Sumit" />
        </StackPanel>
    </Grid>
</UserControl>