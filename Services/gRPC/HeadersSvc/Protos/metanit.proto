syntax = "proto3";

option csharp_namespace = "HeadersSvc";

package greet;

message Request{}

message Response{
  string content = 1;
}

service Messenger{
  rpc SendMessage (Request) returns (Response);
}